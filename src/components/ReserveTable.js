import ReserveTableButton from './ReserveTableButton';

import './ReserveTable.css';

const ReserveTable = () => {
    return(
        <>
            <section className="main-reserve">
                <section className="reserve-text">
                    <h1 className="reserve-title">Little Lemon</h1>
                    <h2 className="reserve-subtitle">Chicago</h2>
                    <p className="reserve-desc">
                        We are a family owned Mediterranean restaurant,
                        focused on traditional recipes served with a modern
                        twist.
                    </p>
                </section>
            </section>
        </>
    );
}

export default ReserveTable;