import logo from '../Images/logo.svg';

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Little Lemon Logo"></img>
        </header>
    );
}

export default Header;

